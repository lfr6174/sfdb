
# Commits

## 格式

```
type(scope): subject

[body]
```

## type
- feat: 添加會影響用戶的功能
- fix: 修復錯誤行為或糾正效能問題
- refactor: 改善程式碼而不異動最終呈現的功能
- test: 添加或新增測試
- build: CI/CD、Docker、相依軟體的設定等影響部署產物的修改
- docs: 添加或新增程式碼註解、開發文件、網站資訊等
- chore: 其它難以分類之瑣事，例如 linter、開發腳本等不影響部署產物的修改


## scope
work, concept, agent 等 app 的修改寫進 subject。開發時，若能分層修改，則盡可能按 scope 拆分 commits。若無法拆分或是無關部署結果，則不用設定 scope。
  
- backend：影響後端平台部署的修改。
- frontend：影響前端平台部署的修改。
- admin：影響管理員後台的修改。


## body

當程式碼修改的語境較為複雜時需要填寫 body，例如：
- 需要程式碼異動外的指令操作時，例如 migration、添加環境變項、重啟伺服器等
- 在不同方案間取捨，最終選定其中一種方案時
- 採用了某種權宜措施時
- 修改動機無法從程式碼看出來時
